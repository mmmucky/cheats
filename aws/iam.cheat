% aws, iam

### Authentication
# Print long-lived access key in aws-vault
aws-vault exec PROFILE --no-session -- env | grep -E 'AWS_(ACCESS|SECRET|SESSION)_'

### SSO
# Sign in with SSO
aws sso login

### ARNs
# ARN Structure
echo arn:partition:service:region:account-id:resource
